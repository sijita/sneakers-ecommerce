export default function ActiveSession({ title }) {
  return (
    <div className="h-screen flex items-center justify-center">
      <p className="text-center text-3xl font-bold">{title}</p>
    </div>
  );
}
